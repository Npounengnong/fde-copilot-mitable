---
name: mitable-get-started
description: Walk the user through their first interaction with Mitable. Loads the example customer, shows a brief, points at how to add their own.
---

# /mitable-get-started

The user has invoked the get-started walkthrough. Delegate to the `mitable-get-started` skill — it knows how to adapt based on whether the user is fresh or returning.

Don't improvise the walkthrough here. The skill owns the script.
